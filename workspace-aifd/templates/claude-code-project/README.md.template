# {project_name}

> 由 AIFD 框架初始化。

## 技术栈
{tech_stack}

## 目录结构（业务项目）
- `.claude/`
  - `skills/`（供用户扩展）
  - `agents/`（pm-agent、arch-agent、be-agent、fe-agent、qa-agent、devops-agent）
  - `commands/`（供用户扩展）
  - `hooks/`（供用户扩展）
- `CLAUDE.md` Claude Code 上下文文件
- `workspace/` AI 工作区（临时文件、中间产物）
- `docs/`
  - `specs/` 全量规格与 featureXXX-specs
  - `knowledges/` 模板、规范、领域知识、经验沉淀
- `frontend/` 前端代码
- `backend/` 后端代码
- `testing/` 测试代码（integration/e2e/performence/reports）
- `devops/` CI/CD 代码
